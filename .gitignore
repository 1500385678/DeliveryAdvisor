# DeliveryWeb · .gitignore · 2026-08-28 补
# 用途:屏蔽本地编辑器/系统噪声,避免污染 git 历史
# 维护:T4 文档/结构任务,本顾问不主动 push .DS_Store 类文件

# macOS 系统文件
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows 系统文件
Thumbs.db
ehthumbs.db
Desktop.ini

# 编辑器临时文件
*.swp
*.swo
*~
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Python(Phase 1+ 启用 md_to_json.py 等脚本)
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/

# Node(Phase 2+ 启用 React/Vite)
node_modules/
dist/
build/
.npm/
.yarn/

# 日志/临时
*.log
*.tmp
*.bak
*.orig
# 白名单:macOS 大小写不敏感下 `*.log` 会把 `.Log/` 目录本身也匹配掉
#(导致 .Log/*.md 巡检报告无法用 `git add -A` 常规添加,需 inspect 脚本用 `git add -f` 强制)
# 此处显式 re-include 让 .Log/ 目录及 .md 巡检报告可正常入库
!.Log/
!.Log/*.md

# 环境变量
.env
.env.local
.env.*.local

# .plan/ 当日临时计划(commit 后删除,文件不入版本)
.plan/*.md
!.plan/.gitkeep
